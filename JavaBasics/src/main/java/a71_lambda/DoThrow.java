package a71_lambda;

public class DoThrow {

//    public static void main(String[] args) {
//        AuthenticationRequestEntity request = dao.findById(stamp)
//                .orElseThrow(() -> new NotFoundException("Authentication request with ID:" + stamp + " was not found"));
//    }
}
